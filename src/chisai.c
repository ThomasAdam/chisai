// Chisai - Window Manager
// Imports
#include <xcb/xcb.h>
#include <stdbool.h>

#define MAX(a, b) ((a > b) ? (a) : (b))

// Function Signatures

// Main Event Loop
int
main(void)
{
    while(true)
    {
        
    }
}

